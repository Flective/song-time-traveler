# Listen page — polish pass TODO (deferred, not urgent)

Captured end of session 2026-06-11. These are design-polish items, parked deliberately
while functionality leads. Address in a later polish pass.

## Card layout inconsistencies
- **Play button position differs** between card types: triangulated cards put it top-right;
  single (compact) cards put it inline-left. Jarring. Unify the position across both.
- **Cover image size** feels small relative to the lyrics lines beside it. Consider enlarging
  the song-art thumbnail so it balances the text column. (Currently 130px.)

## Open design question (NOT decided — discuss, don't force)
- **What should clicking the cover image do?** Currently nothing. Options to consider:
  open larger / play the song / nothing. Jim unsure — leave until decided.
- **Per-song vs per-rendition thumbnail:** currently generative art is per-rendition (changes
  with angle). Jim floated maybe one image per song instead. Undecided. Revisit when real
  artwork exists.

## Note
Jim's own read at session end: "maybe I'm complicating things too much for right now —
functionality is more important." Correct call. These are deferred on purpose.
